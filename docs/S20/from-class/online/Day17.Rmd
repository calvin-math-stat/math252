---
pagetitle: 'M252: 5/06/2020'
output: 
  html_document:
    includes:
      before_body: ../../header.shtml
    css: ['../../../styles/rpruim2017a.css', '../../../styles/folding.css']
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(ggformula)
library(dplyr)
theme_set(theme_bw())
```

\def\prob{p}
\def\Prob{p}
\def\intersect{\cap}
\def\union{\cup}
\def\To{\Rightarrow}
\def\mybold#1{\boldsymbol{#1}}


<script src="../../../styles/folding.js"></script>

### Daily Dope Sheet 

## Wednesday, May 6: Turing Machines

#### Homework

* Due today:  PS 17 (submit via [gradescope](https://gradescope.com))
* Due Monday:  PS 18 (submit via [gradescope](https://gradescope.com))

#### Worksheets

Models of Computation

* [HTML](../models-of-computation/turing-machines)
* [PDF](../models-of-computation/Models-of-Computation.pdf)

The goal is to get through Exercise 5. Next time we will learn how to
create our own Turing machines.

### Solutions/Hints/etc.

<div class = "foldable" label = "Essential Ingredients"> 

The three essential ingredients are 

1. Read
2. Write
3. Remember

Implicit in these is a fourth:

4. Locate/Move

since we need to be able to write in different places and read from different
places.

The Turing machine model is based on treating these as the fundamental
operations of calculating (ie, of performing a caclculation). And this 
matches well both

* pencil-and-paper arithmetic performed by humans, and
* what happens in a modern computer (which didn't even exist at the time
Turing machines were first developed).

</div>


<div class = "foldable" label = "Examples in 9.4"> 
You can check these using the simulator at
<http://morphett.info/turing/turing.html>.
</div>

<div class = "foldable" label = "Exercise 4"> 
If $x\not\in L(M)$, $M$ on input $x$ might halt in a rejecting state or it might
never halt.
</div>

<div class = "foldable" label = "Exercise 5"> 

The language consisting of all strings that have an even number of 0's 
and an even number of 1's.

</div>

